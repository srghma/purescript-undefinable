package:
  name: undefinable
  description: A library for dealing with undefined javascript variables
  publish:
    license: MIT
    version: 4.0.0
    location:
      githubOwner: ethul
      githubRepo: purescript-undefinable
  dependencies:
    - functions
    - maybe
    - prelude
    - unsafe-coerce
  test:
    main: Test.Main
    dependencies:
      - assert
      - effect
workspace:
  packageSet:
    registry: 64.4.0
  extraPackages: {}
